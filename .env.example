# n8n-vet development environment
# Copy to .env and fill in values.
# .env is gitignored — never commit secrets.

# Required for execution-backed validation and integration tests
N8N_HOST=http://localhost:5678
N8N_API_KEY=
